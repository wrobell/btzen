dist: xenial
language: python
sudo: required
python:
  - "3.7-dev"
install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libsystemd-dev
  - pip install 'cython>=0.29.4'
script:
  - pip install .
